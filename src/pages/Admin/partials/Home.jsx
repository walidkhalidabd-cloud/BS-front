import React, { useEffect, useState } from "react";
import Loading from "../../../components/Shared/Loading";



const Home = () => {
  const title = "الصفحة الرئيسية لمدير النظام"
  return(
        <h3 className="text-warning">{title}</h3>

  )
};

export default Home;
